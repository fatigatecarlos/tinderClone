import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  image: {
    width: 200,
    height: 100,
    resizeMode: 'contain',
    alignSelf: 'center',
    marginBottom: 20,
  },
});

export default style;
